<!-- index.html -->
<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Ana Sayfa</title>
  <link rel="icon" type="image/jpeg" href="img/logo.jpg">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">
  <style>
    /* CSS Reset */
    *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }
    html { font-size: 16px; scroll-behavior: smooth; }
    body { font-family: 'Inter', sans-serif; line-height: 1.6; color: #333; background: #f9fbfd; }
    img { max-width: 100%; display: block; }
    a { text-decoration: none; color: inherit; }
    ul { list-style: none; }

    /* Variables */
    :root {
      --clr-primary: #00234e;
      --clr-secondary: #0062ff;
      --clr-light: #ffffff;
      --clr-dark: #0a0a0a;
      --clr-gray: #f1f5f9;
      --transition: all 0.3s ease;
      --radius: 8px;
    }

/* Navbar */
    header {
      position: fixed;
      top: 0;
      width: 100%;
      background: var(--clr-light);
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
      z-index: 1000;
    }
    .nav-container {
      max-width: 1200px;
      margin: auto;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 1rem;
    }
    .logo {
      font-size: 1.75rem;
      font-weight: 700;
      color: var(--clr-primary);
    }
    nav {
      display: flex;
      align-items: center;
    }
    nav ul {
      display: flex;
      gap: 2rem;
    }
    nav a {
      font-weight: 500;
      transition: var(--transition);
    }
    nav a:hover {
      color: var(--clr-secondary);
    }
    .hamburger {
      display: none;
      font-size: 1.5rem;
      cursor: pointer;
    }

    /* Mobile Nav */
    .mobile-nav {
      position: absolute;
      top: 100%;
      left: 0;
      width: 100%;
      background: var(--clr-light);
      display: none;
      flex-direction: column;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }
    .mobile-nav.show {
      display: flex;
    }
    .mobile-nav a {
      padding: 1rem 2rem;
      border-bottom: 1px solid #eee;
    }

    /* Responsive */
    @media (max-width: 768px) {
      nav ul {
        display: none;
      }
      .hamburger {
        display: block;
      }
    }
    /* Hero */
    .hero { display: flex; align-items: center; justify-content: center; height: 80vh; background: url('https://source.unsplash.com/1600x900?storage') center/cover no-repeat; position: relative; }
    .hero::before { content: ''; position: absolute; inset: 0; background: rgba(0,0,0,0.5); }
    .hero-content { position: relative; text-align: center; color: var(--clr-light); max-width: 600px; }
    .hero-content h1 { font-size: 3rem; margin-bottom: 1rem; font-weight: 700; }
    .hero-content p { font-size: 1.125rem; margin-bottom: 2rem; }
    .btn { display: inline-block; padding: 0.75rem 1.75rem; background: var(--clr-secondary); color: var(--clr-light); border-radius: var(--radius); font-weight: 600; transition: var(--transition); }
    .btn:hover { background: var(--clr-primary); }

    /* Features Section */
    .features { background: var(--clr-gray); padding: 4rem 2rem; }
    .features .grid { display: grid; grid-template-columns: repeat(auto-fit,minmax(250px,1fr)); gap: 2rem; max-width: 1200px; margin: auto; }
    .feature-card { background: var(--clr-light); padding: 2rem; border-radius: var(--radius); box-shadow: 0 2px 12px rgba(0,0,0,0.05); text-align: center; transition: var(--transition); }
    .feature-card:hover { transform: translateY(-5px); }
    .feature-card i { font-size: 2.5rem; color: var(--clr-secondary); margin-bottom: 1rem; }
    .feature-card h3 { font-size: 1.25rem; margin-bottom: 0.75rem; }
    .feature-card p { font-size: 1rem; color: #555; }

    /* Depo Card Section */
    .depolar { padding: 4rem 2rem; max-width: 1200px; margin: auto; }
    .depolar .grid { display: grid; grid-template-columns: repeat(auto-fit,minmax(280px,1fr)); gap: 2rem; }
    .depo-card { background: var(--clr-light); border-radius: var(--radius); overflow: hidden; box-shadow: 0 4px 16px rgba(0,0,0,0.1); transition: var(--transition); }
    .depo-card:hover { transform: translateY(-8px); box-shadow: 0 6px 24px rgba(0,0,0,0.15); }
    .depo-card img { height: 200px; object-fit: cover; }
    .depo-card .content { padding: 1.5rem; }
    .depo-card h4 { font-size: 1.5rem; margin-bottom: 0.75rem; color: var(--clr-primary); }
    .depo-card ul { list-style: disc inside; color: #555; margin-bottom: 1rem; }
    .depo-card ul li { margin-bottom: 0.5rem; }
    .depo-card .btn-detail { font-size: 0.9rem; padding: 0.5rem 1.25rem; }

    /* Contact & Footer */

    .social { display: flex; justify-content: center; gap: 1.5rem; margin-top: 1rem; }
    .social a { font-size: 2rem; color: white; transition: var(--transition); }
    .social a:hover { color: var(--clr-secondary); }

    footer { background:var(--clr-primary); color:var(--clr-light); text-align:center; padding:1.5rem; margin-top:2rem; }

    /* Responsive */
    @media (max-width: 768px) {
      nav ul { display: none; }
      .hamburger { display: block; }
      .mobile-nav { position: absolute; top: 100%; left: 0; width: 100%; background: var(--clr-light); display: none; flex-direction: column; box-shadow: 0 2px 8px rgba(0,0,0,0.1); }
      .mobile-nav.show { display: flex; }
      .mobile-nav a { padding: 1rem 2rem; border-bottom: 1px solid #eee; }
    }

        /* Map */
    .map h2 { text-align:center; margin-bottom:2rem; color:var(--primary); }
    .map iframe { width:100%; height:350px; border:none; box-shadow:0 2px 12px rgba(0,0,0,0.1); }
  </style>
</head>
<body>
  <!-- Navbar -->
  <header>
    <div class="nav-container">
      <div class="logo">DepoTut</div>
      <nav>
        <ul>
          <li><a href="#hero">Ana Sayfa</a></li>
          <li><a href="#features">Özellikler</a></li>
          <li><a href="#depolar">Depolar</a></li>
          <li><a href="#contact">İletişim</a></li>
        </ul>
      </nav>
      <i class="fas fa-bars hamburger" onclick="toggleNav()"></i>
    </div>
    <div class="mobile-nav" id="mobileNav">
      <a href="#hero" onclick="toggleNav()">Ana Sayfa</a>
      <a href="#features" onclick="toggleNav()">Özellikler</a>
      <a href="#depolar" onclick="toggleNav()">Depolar</a>
      <a href="#contact" onclick="toggleNav()">İletişim</a>
    </div>
  </header>


  <!-- Hero -->
  <section class="hero" id="hero">
    <div class="hero-content">
      <h1>Depolarınızı En İyi Şekilde Yönetin</h1>
      <p>Modern tesislerimizle eşyalarınız güvende, erişim her zaman elinizin altında.</p>
      <a href="#depolar" class="btn">Depoları Keşfet</a>
    </div>
  </section>

  <!-- Features -->
  <section class="features" id="features">
    <div class="grid">
      <div class="feature-card">
        <i class="fas fa-shield-alt"></i>
        <h3>7/24 Güvenlik</h3>
        <p>Kamera ve canlı izleme sistemleri ile maksimum koruma.</p>
      </div>
      <div class="feature-card">
        <i class="fas fa-snowflake"></i>
        <h3>İklim Kontrollü</h3>
        <p>Eşyalarınız için ideal sıcaklık ve nem dengesi.</p>
      </div>
      <div class="feature-card">
        <i class="fas fa-box-open"></i>
        <h3>Ambalaj Desteği</h3>
        <p>Taşıma ve paketleme malzemeleri sağlıyoruz.</p>
      </div>
      <div class="feature-card">
        <i class="fas fa-key"></i>
        <h3>Kişisel Erişim</h3>
        <p>Elektronik kart ve şifreli giriş sistemi.</p>
      </div>
    </div>
  </section>

  <!-- Depo Cards -->
  <section class="depolar" id="depolar">
    <h2 style="text-align:center; margin-bottom:2rem; color: var(--clr-primary);">Depo Seçeneklerimiz</h2>
    <div class="grid">
      <div class="depo-card">
        <img src="https://source.unsplash.com/400x300?mini-storage" alt="9 m² Depo">
        <div class="content">
          <h4>9 m² Depo</h4>
          <ul>
            <li>200 kg taşıma kapasitesi</li>
            <li>Kamera ve alarm</li>
            <li>7/24 erişim</li>
          </ul>
          <a href="/depo9" class="btn btn-detail">Detaylı İncele</a>
        </div>
      </div>
      <div class="depo-card">
        <img src="https://source.unsplash.com/400x300?warehouse-interior" alt="16 m² Depo">
        <div class="content">
          <h4>16 m² Depo</h4>
          <ul>
            <li>500 kg taşıma kapasitesi</li>
            <li>İklim kontrollü</li>
            <li>Ambalaj desteği</li>
          </ul>
          <a href="/depo16" class="btn btn-detail">Detaylı İncele</a>
        </div>
      </div>
    </div>
  </section>

<!-- Footer -->
  <footer>

  <section class="map" id="map">
    <h2>Konum Haritamız</h2>
    <iframe src="https://www.google.com/maps/embed?pb=!1m17!1m12!1m3!1d65946.1185144488!2d27.732177980322273!3d37.195160787589685!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m2!1m1!2zMzfCsDEyJzE5LjQiTiAyN8KwNDQnMDEuNyJF!5e0!3m2!1str!2str!4v1753199863658!5m2!1str!2str" width="600" height="450" style="border:0;" allowfullscreen="" loading="lazy" referrerpolicy="no-referrer-when-downgrade"></iframe>
  </section>

  <!-- Contact -->
  <section class="contact" id="contact">
    <h2>Bizimle İletişime Geçin</h2>
    <p>Sosyal medya ve anlık mesajlaşma ile hızlı destek.</p>
    <div class="social">
      <a href="https://www.instagram.com/depo.tut" target="_blank"><i class="fab fa-instagram"></i></a>
      <a href="https://wa.me/+905423484165" target="_blank"><i class="fab fa-whatsapp"></i></a>
      <a href="mailto:depotuut@gmail.com" target="_blank"><i class="fas fa-envelope"></i></a>
    </div>

  
    <p>&copy; 2025 created by Baran Çelik | DepoTut | Tüm Hakları Saklıdır</p>
  </footer>

  <script>
    function toggleNav() {
      document.getElementById('mobileNav').classList.toggle('show');
    }
  </script>
</body>
</html>